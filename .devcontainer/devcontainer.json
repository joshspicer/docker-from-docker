{
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu",
	"features": {
		"ghcr.io/devcontainers/features/docker-from-docker:1": {}
	}
	// -- Add as an interactive TTY command when that ships.
	// "postStartCommand": "docker run -it --rm -v /:/host ubuntu:latest"
}
